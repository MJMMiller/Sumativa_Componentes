import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'A3_S3_Miller_3E1',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
